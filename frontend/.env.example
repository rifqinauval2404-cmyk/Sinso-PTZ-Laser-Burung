# Must match backend/.env's API_KEY - baked into the client bundle at build time (Vite
# exposes VITE_-prefixed vars to the browser), so this is only "basic protection" against
# casual access, not a secret that can stay confidential in a public build.
VITE_API_KEY=change-me
